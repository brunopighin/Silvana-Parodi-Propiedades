{
  "name": "silvana-parodi-propiedades",
  "version": "1.0.0",
  "description": "Sistema inmobiliario - Silvana Parodi Propiedades",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd frontend && npm install --include=dev && npm run build",
    "setup": "cd backend && npm install && npx prisma generate && npx prisma db push && node prisma/seed.js",
    "install:all": "cd backend && npm install && cd ../frontend && npm install --include=dev",
    "deploy": "npm run build && npm run setup"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
